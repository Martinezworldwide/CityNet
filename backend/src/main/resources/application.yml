spring:
  application:
    name: CityNetBackend

server:
  port: ${PORT:8886} # Bind to Render's PORT if present; default to 8886 for local runs

citynet:
  graph:
    # This section is intentionally empty to respect the data authority rule.
    # Users should provide real road network data from authoritative sources
    # (for example, via configuration management or separate ingestion tools).
    edges: [] # Real edges can be injected here by the user or external systems.

